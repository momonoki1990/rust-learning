# CLAUDE.md

Claude Codeがこのプロジェクトで作業する際の指示・コンテキストです。

## プロジェクト概要

Rustの基礎文法を体系的に学ぶための学習プロジェクト。  
各セクションは独立したモジュール（`.rs`ファイル）として実装されている。

## ディレクトリ構成

```
rust_basics/
├── src/
│   ├── main.rs           # エントリポイント・各セクションの呼び出し
│   ├── structs.rs        # セクション5: 構造体とメソッド
│   ├── enums.rs          # セクション6: 列挙型とパターンマッチング
│   ├── traits.rs         # セクション7: トレイト
│   ├── lifetimes.rs      # セクション8: ライフタイム
│   ├── closures.rs       # セクション9: クロージャとイテレータ
│   └── error_handling.rs # セクション10: エラーハンドリング
├── Cargo.toml
└── README.md
```

## よく使うコマンド

```bash
cargo run    # 全セクション実行
cargo check  # コンパイルチェックのみ（実行しない）
cargo build  # ビルドのみ
cargo test   # テスト実行
```

## 作業ルール

- コメントは日本語で書く
- 新しいセクションを追加するときは以下を必ず行う:
  1. `src/` に新しい `.rs` ファイルを作成
  2. `main.rs` に `mod` 宣言と `run()` 呼び出しを追加
  3. `README.md` のセクション一覧を更新
- 実装後は `cargo run` で動作確認してからコミット
- 学習用途のため、わかりやすさを優先したコードを書く

## 学習スタイル

- ユーザーへの説明はGoとの比較を交えると理解しやすい
- 重要な実装箇所はユーザー自身に書いてもらう（Learn by Doing）
- `TODO(human)` コメントで実装箇所を明示する
